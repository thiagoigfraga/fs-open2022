1- When clicking on submit, the user will be triggering an event handler.
2- The event handler that will read the form that is being sent with the 'POST' method at the address '/exampleapp/new_note'.
3 - Inside the server, the content that was sent by 'POST' will be handled, adding the body of the request and the date the note was sent.
4 - Using the DOM API, creating a new LI element and placing it inside the list with the other elements.
5 - The request gives the redirect warning
